/**
 * The role of a given {@link Counter}.
 *
 * `Private`s can only move forward, but `Officer`s are able to go to any direction.
 */
export const enum Role {
    Officer = "Officer",
    Private = "Private"
}
